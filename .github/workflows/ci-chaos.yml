name: ci-chaos

on:
  workflow_dispatch:

jobs:
  chaos-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Print guidance
        run: |
          echo "This workflow is a placeholder for chaos smoke tests."
          echo "Run scripts/bash/chaos_smoke.sh on an environment with docker access."
