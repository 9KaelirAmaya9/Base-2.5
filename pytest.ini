[pytest]
# Default test run should be self-contained.
# Integration/perf tests require external services (Postgres/Redis/Celery) and are
# meant to be run explicitly (e.g. `pytest -m integration`).
addopts = -m "not integration and not perf"

markers =
    integration: requires external services (postgres/redis/celery)
    perf: performance-sensitive tests
